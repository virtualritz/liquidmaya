export LIQUIDHOME=/media/Trekstor/alf/dev/sourceforge/liquid
export LIQUID_GLOBALS_OVERRIDE=${LIQUIDHOME}/renderers

if [[ -n $MAYA_SCRIPT_PATH ]] ;
then export MAYA_SCRIPT_PATH=${LIQUIDHOME}/mel:${MAYA_SCRIPT_PATH}
else export MAYA_SCRIPT_PATH=${LIQUIDHOME}/mel
fi

if [[ -n $MAYA_PLUG_IN_PATH ]] ;
then export MAYA_PLUG_IN_PATH=${LIQUIDHOME}/bin/debug/3delight/2009:${MAYA_PLUG_IN_PATH}
else export MAYA_PLUG_IN_PATH=${LIQUIDHOME}/bin/debug/3delight/2009
fi

if [[ -n $XBMLANGPATH ]] ;
then export XBMLANGPATH=${LIQUIDHOME}/icons:${XBMLANGPATH}
else export XBMLANGPATH=${LIQUIDHOME}/icons
fi

export PATH=${LIQUIDHOME}/bin/debug/3delight/2009:${LIQUIDHOME}/scripts:$PATH


